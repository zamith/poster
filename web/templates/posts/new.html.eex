<h2>New post</h2>

<%= render "form.html",
           changeset: @changeset,
           categories: @categories,
           action: posts_path(@conn, :create) %>

<%= link "Back", to: posts_path(@conn, :index) %>
